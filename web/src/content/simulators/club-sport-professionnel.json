{
  "slug": "club-sport-professionnel",
  "activity": "club de sport professionnel",
  "intro": "Tarifs réels du barème SACEM 2026 (autorisation n° 83). Forfait par rencontre à domicile selon capacité et type de diffusion.",
  "questions": [
    {
      "id": "rencontres",
      "label": "Nombre de rencontres à domicile par saison",
      "type": "number",
      "default": 17,
      "min": 1,
      "max": 60,
      "step": 1,
      "unit": "rencontres"
    },
    {
      "id": "capacite",
      "label": "Capacité de l'enceinte",
      "type": "select",
      "default": 10000,
      "options": [
        { "value": 2500, "label": "Moins de 2 500 places" },
        { "value": 5000, "label": "2 500 à 5 000" },
        { "value": 10000, "label": "5 001 à 10 000" },
        { "value": 20000, "label": "10 001 à 20 000" },
        { "value": 30000, "label": "20 001 à 30 000" },
        { "value": 40000, "label": "30 001 à 40 000" },
        { "value": 50000, "label": "Plus de 40 000" }
      ]
    },
    {
      "id": "diffusion",
      "label": "Type de diffusion",
      "type": "radio",
      "default": "animations",
      "options": [
        { "value": "sonorisation", "label": "Sonorisation (fond sonore)" },
        { "value": "animations", "label": "Animations (DJ, jingles, écrans, cheerleading)" }
      ]
    },
    {
      "id": "toutes_sonorisees",
      "label": "Toutes les rencontres du championnat sont sonorisées (-15 %)",
      "type": "boolean",
      "default": true
    },
    {
      "id": "budget_petit",
      "label": "Budget club < 5 M€ (-10 %)",
      "type": "boolean",
      "default": false
    },
    {
      "id": "abonnement",
      "label": "Prix d'abonnement annuel",
      "type": "radio",
      "default": "moyen",
      "options": [
        { "value": "bas", "label": "< 100 € (-20 %)" },
        { "value": "moyen", "label": "100 à 150 € (base)" },
        { "value": "haut", "label": "> 150 € (+15 %)" }
      ]
    },
    {
      "id": "activite_receptive",
      "label": "Activité réceptive (loges VIP, soirées partenaires) +50 %",
      "type": "boolean",
      "default": false
    },
    {
      "id": "tarif_reduit",
      "label": "Je bénéficie du tarif réduit (contrat général SACEM signé dans les 15 jours)",
      "type": "boolean",
      "default": false
    }
  ],
  "compute": {
    "kind": "formula",
    "expr": "rencontres * (diffusion == 'animations' ? (capacite == 2500 ? 171.19 : capacite == 5000 ? 228.25 : capacite == 10000 ? 285.31 : capacite == 20000 ? 399.44 : capacite == 30000 ? 513.56 : capacite == 40000 ? 684.76 : 1027.12) : (capacite == 2500 ? 85.60 : capacite == 5000 ? 114.12 : capacite == 10000 ? 142.65 : capacite == 20000 ? 199.72 : capacite == 30000 ? 256.78 : capacite == 40000 ? 342.38 : 513.56)) * (toutes_sonorisees ? 0.85 : 1) * (budget_petit ? 0.90 : 1) * (abonnement == 'bas' ? 0.80 : abonnement == 'haut' ? 1.15 : 1) * (activite_receptive ? 1.50 : 1)",
    "modifiers": [
      { "kind": "multiply", "value": 0.8, "when": { "tarif_reduit": true } }
    ]
  },
  "spre": {
    "compute": {
      "kind": "formula",
      "expr": "sacem * 0.65",
      "min": 110.60
    }
  },
  "footnote": "SACEM au tarif général par défaut, saison 2026-2027. Budget artistique plafonné à 3 000 € par rencontre pour animations/réceptif. Bars, boutiques et hébergements dans l'enceinte relèvent d'autres barèmes."
}
